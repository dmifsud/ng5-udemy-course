import { IsShowingDirective } from './is-showing.directive';

describe('IsShowingDirective', () => {
  it('should create an instance', () => {
    const directive = new IsShowingDirective();
    expect(directive).toBeTruthy();
  });
});
